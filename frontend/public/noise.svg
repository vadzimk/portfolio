<svg viewBox="0 0 200 200">
    <filter id="a">
        <feTurbulence baseFrequency=".65" numOctaves="3" stitchTiles="stitch" type="fractalNoise"/>
    </filter>
    <rect width="100%" height="100%" filter="url(#a)"/>
</svg>